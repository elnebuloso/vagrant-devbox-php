# ansible/config/phing-commons

* add a build.properties file here for using this as /root/phing-commons/build.properties
* if no build.properties file is present here, a default file will be provisioned